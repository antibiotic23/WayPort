<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WayPort Assignment</title>
    <style>
        /* Basic Styling */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f0f5;
            color: #333;
        }
        .navbar {
            background-color: #6a0dad; /* Purple */
            color: #fff;
            padding: 15px;
            text-align: center;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }
        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 30px;
            background-color: #fff;
            border: 1px solid #ddd;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        .container h2 {
            font-size: 32px; /* Updated for consistent size */
            color: #4a00e0; /* Blue */
            margin: 20px 0;
        }
        .button-container {
            justify-content: center; /* Center the button */
            gap: 20px;
        }
        .button {
            background-color: #5D3FD3;
            color: #fff;
            padding: 12px 25px;
            border: none;
            border-radius: 30px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            box-shadow: 0 5px 15px rgba(255, 0, 127, 0.3);
        }
        .button:hover {
            background-color: #33b0ff;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>OTP Implementation</h2> <!-- Title only -->
        <div class="button-container">
            <button class="button" id="login-button" onclick="location.href='/login'">Login with Number and OTP</button> <!-- Updated button -->
        </div>
    </div>
</body>
</html>
